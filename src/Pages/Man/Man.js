import React from 'react'
import Transition from '../../components/Transition'

const Man = ({timeline}) => {
  return (
    <div>
      <Transition timeline={timeline}/>
      Man</div>
  )
}

export default Man